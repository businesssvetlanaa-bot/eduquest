{
  "name": "eduquest",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd client && npm ci --include=dev && npm run build && cd ../server && npm ci --include=dev && npm run build && npx prisma generate",
    "start": "cd server && npx prisma migrate deploy && node dist/index.js"
  }
}
